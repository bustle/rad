import Guid from '../src/delegate/guid'

describe('Guid', () => {
  describe('#last', () => {
    xit('resolves a string id')
  })

  describe('#next', () => {
    xit('resolves a string id')
  })
})
